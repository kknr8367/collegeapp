import 'package:flutter/material.dart';

class CommunicationTools extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Communication Tools'),
      ),
      body: Center(
        child: Text('Communication Tools Screen'),
      ),
    );
  }
}
