import 'package:flutter/material.dart';

class CampusInformation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Campus Information'),
      ),
      body: Center(
        child: Text('Campus Information Screen'),
      ),
    );
  }
}
