import 'package:flutter/material.dart';

class StudentServices extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Student Services'),
      ),
      body: Center(
        child: Text('Student Services Screen'),
      ),
    );
  }
}
